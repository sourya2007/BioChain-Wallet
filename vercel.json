{
  "version": 2,
  "installCommand": "npm install --prefix frontend",
  "buildCommand": "npm run build --prefix frontend && rm -rf app/static && mkdir -p app/static && cp frontend/dist/index.html app/static/index.html && cp -r frontend/dist/assets app/static/assets",
  "outputDirectory": "app/static",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.py"
    }
  ]
}
